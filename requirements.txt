# Python requirements for building the site. Install with `make deps`.
jinja2>=3.0

# Used by `make watch` (build.py --watch) only: rebuilds and refreshes the
# browser on changes. A plain `python3 build.py` never touches it.
livereload>=2.7
